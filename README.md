# Reconstructed baseline (not the product)

Path-renamed snapshot of SivaramPg/solana-simple-prediction-market-contract
at `3353bc02ca005eca73725a7231ff1578087a2eaa`. Crate `prediction_market` → `fomo_pnl` only.

This commit exists so `git diff` against the next commit shows the product
changes. It is not a claim that this working tree was checked out from that SHA.
