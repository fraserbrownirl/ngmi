# Splash

Parked static marketing site. **https://ngmi.markets** is the Next.js tote (`ngmi-stage`).

Vercel project: **`ngmi`** (`prj_RVPkHN5tGZFTCrte7DMZMMrkiMp0`). Deploy with `pnpm deploy` in this directory only if you intend to ship splash to this unused project — that must not steal `ngmi.markets`.

**https://ngmi-stage.vercel.app** is the same tote as production. Never deploy this folder there.
