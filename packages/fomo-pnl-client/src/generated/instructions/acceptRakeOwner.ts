/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findRakePda } from "../pdas";
import { FOMO_PNL_PROGRAM_ADDRESS } from "../programs";

export const ACCEPT_RAKE_OWNER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([25, 83, 107, 71, 168, 188, 176, 89]);

export function getAcceptRakeOwnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_RAKE_OWNER_DISCRIMINATOR,
  );
}

export type AcceptRakeOwnerInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountPending extends string | AccountMeta<string> = string,
  TAccountRake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPending extends string
        ? ReadonlySignerAccount<TAccountPending> &
            AccountSignerMeta<TAccountPending>
        : TAccountPending,
      TAccountRake extends string
        ? WritableAccount<TAccountRake>
        : TAccountRake,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptRakeOwnerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptRakeOwnerInstructionDataArgs = {};

export function getAcceptRakeOwnerInstructionDataEncoder(): FixedSizeEncoder<AcceptRakeOwnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ACCEPT_RAKE_OWNER_DISCRIMINATOR }),
  );
}

export function getAcceptRakeOwnerInstructionDataDecoder(): FixedSizeDecoder<AcceptRakeOwnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptRakeOwnerInstructionDataCodec(): FixedSizeCodec<
  AcceptRakeOwnerInstructionDataArgs,
  AcceptRakeOwnerInstructionData
> {
  return combineCodec(
    getAcceptRakeOwnerInstructionDataEncoder(),
    getAcceptRakeOwnerInstructionDataDecoder(),
  );
}

export type AcceptRakeOwnerAsyncInput<
  TAccountPending extends string = string,
  TAccountRake extends string = string,
> = {
  pending: TransactionSigner<TAccountPending>;
  rake?: Address<TAccountRake>;
};

export async function getAcceptRakeOwnerInstructionAsync<
  TAccountPending extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: AcceptRakeOwnerAsyncInput<TAccountPending, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptRakeOwnerInstruction<TProgramAddress, TAccountPending, TAccountRake>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pending: { value: input.pending ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.rake.value) {
    accounts.rake.value = await findRakePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pending", accounts.pending),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getAcceptRakeOwnerInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptRakeOwnerInstruction<
    TProgramAddress,
    TAccountPending,
    TAccountRake
  >);
}

export type AcceptRakeOwnerInput<
  TAccountPending extends string = string,
  TAccountRake extends string = string,
> = {
  pending: TransactionSigner<TAccountPending>;
  rake: Address<TAccountRake>;
};

export function getAcceptRakeOwnerInstruction<
  TAccountPending extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: AcceptRakeOwnerInput<TAccountPending, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): AcceptRakeOwnerInstruction<TProgramAddress, TAccountPending, TAccountRake> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pending: { value: input.pending ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pending", accounts.pending),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getAcceptRakeOwnerInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptRakeOwnerInstruction<
    TProgramAddress,
    TAccountPending,
    TAccountRake
  >);
}

export type ParsedAcceptRakeOwnerInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pending: TAccountMetas[0];
    rake: TAccountMetas[1];
  };
  data: AcceptRakeOwnerInstructionData;
};

export function parseAcceptRakeOwnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptRakeOwnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pending: getNextAccount(), rake: getNextAccount() },
    data: getAcceptRakeOwnerInstructionDataDecoder().decode(instruction.data),
  };
}
