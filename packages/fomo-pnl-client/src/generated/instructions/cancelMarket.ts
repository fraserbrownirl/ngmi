/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findMarketPda } from "../pdas";
import { FOMO_PNL_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_MARKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  205, 121, 84, 210, 222, 71, 150, 11,
]);

export function getCancelMarketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_MARKET_DISCRIMINATOR,
  );
}

export type CancelMarketInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountResolver extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountResolver extends string
        ? ReadonlySignerAccount<TAccountResolver> &
            AccountSignerMeta<TAccountResolver>
        : TAccountResolver,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type CancelMarketInstructionData = {
  discriminator: ReadonlyUint8Array;
  marketId: bigint;
};

export type CancelMarketInstructionDataArgs = { marketId: number | bigint };

export function getCancelMarketInstructionDataEncoder(): FixedSizeEncoder<CancelMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["marketId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_MARKET_DISCRIMINATOR }),
  );
}

export function getCancelMarketInstructionDataDecoder(): FixedSizeDecoder<CancelMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["marketId", getU64Decoder()],
  ]);
}

export function getCancelMarketInstructionDataCodec(): FixedSizeCodec<
  CancelMarketInstructionDataArgs,
  CancelMarketInstructionData
> {
  return combineCodec(
    getCancelMarketInstructionDataEncoder(),
    getCancelMarketInstructionDataDecoder(),
  );
}

export type CancelMarketAsyncInput<
  TAccountResolver extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
> = {
  resolver: TransactionSigner<TAccountResolver>;
  config?: Address<TAccountConfig>;
  market?: Address<TAccountMarket>;
  marketId: CancelMarketInstructionDataArgs["marketId"];
};

export async function getCancelMarketInstructionAsync<
  TAccountResolver extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: CancelMarketAsyncInput<
    TAccountResolver,
    TAccountConfig,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelMarketInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountConfig,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    resolver: { value: input.resolver ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.market.value) {
    accounts.market.value = await findMarketPda({
      marketId: getNonNullResolvedInstructionInput("marketId", args.marketId),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("resolver", accounts.resolver),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
    ],
    data: getCancelMarketInstructionDataEncoder().encode(
      args as CancelMarketInstructionDataArgs,
    ),
    programAddress,
  } as CancelMarketInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountConfig,
    TAccountMarket
  >);
}

export type CancelMarketInput<
  TAccountResolver extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
> = {
  resolver: TransactionSigner<TAccountResolver>;
  config: Address<TAccountConfig>;
  market: Address<TAccountMarket>;
  marketId: CancelMarketInstructionDataArgs["marketId"];
};

export function getCancelMarketInstruction<
  TAccountResolver extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: CancelMarketInput<TAccountResolver, TAccountConfig, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): CancelMarketInstruction<
  TProgramAddress,
  TAccountResolver,
  TAccountConfig,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    resolver: { value: input.resolver ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("resolver", accounts.resolver),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
    ],
    data: getCancelMarketInstructionDataEncoder().encode(
      args as CancelMarketInstructionDataArgs,
    ),
    programAddress,
  } as CancelMarketInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountConfig,
    TAccountMarket
  >);
}

export type ParsedCancelMarketInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    resolver: TAccountMetas[0];
    config: TAccountMetas[1];
    market: TAccountMetas[2];
  };
  data: CancelMarketInstructionData;
};

export function parseCancelMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      resolver: getNextAccount(),
      config: getNextAccount(),
      market: getNextAccount(),
    },
    data: getCancelMarketInstructionDataDecoder().decode(instruction.data),
  };
}
