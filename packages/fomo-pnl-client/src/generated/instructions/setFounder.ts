/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findRakePda } from "../pdas";
import { FOMO_PNL_PROGRAM_ADDRESS } from "../programs";

export const SET_FOUNDER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  160, 164, 27, 9, 30, 8, 85, 180,
]);

export function getSetFounderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_FOUNDER_DISCRIMINATOR);
}

export type SetFounderInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountFounder extends string | AccountMeta<string> = string,
  TAccountRake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFounder extends string
        ? ReadonlySignerAccount<TAccountFounder> &
            AccountSignerMeta<TAccountFounder>
        : TAccountFounder,
      TAccountRake extends string
        ? WritableAccount<TAccountRake>
        : TAccountRake,
      ...TRemainingAccounts,
    ]
  >;

export type SetFounderInstructionData = {
  discriminator: ReadonlyUint8Array;
  newFounder: Address;
};

export type SetFounderInstructionDataArgs = { newFounder: Address };

export function getSetFounderInstructionDataEncoder(): FixedSizeEncoder<SetFounderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newFounder", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_FOUNDER_DISCRIMINATOR }),
  );
}

export function getSetFounderInstructionDataDecoder(): FixedSizeDecoder<SetFounderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newFounder", getAddressDecoder()],
  ]);
}

export function getSetFounderInstructionDataCodec(): FixedSizeCodec<
  SetFounderInstructionDataArgs,
  SetFounderInstructionData
> {
  return combineCodec(
    getSetFounderInstructionDataEncoder(),
    getSetFounderInstructionDataDecoder(),
  );
}

export type SetFounderAsyncInput<
  TAccountFounder extends string = string,
  TAccountRake extends string = string,
> = {
  founder: TransactionSigner<TAccountFounder>;
  rake?: Address<TAccountRake>;
  newFounder: SetFounderInstructionDataArgs["newFounder"];
};

export async function getSetFounderInstructionAsync<
  TAccountFounder extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: SetFounderAsyncInput<TAccountFounder, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetFounderInstruction<TProgramAddress, TAccountFounder, TAccountRake>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    founder: { value: input.founder ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.rake.value) {
    accounts.rake.value = await findRakePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("founder", accounts.founder),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getSetFounderInstructionDataEncoder().encode(
      args as SetFounderInstructionDataArgs,
    ),
    programAddress,
  } as SetFounderInstruction<TProgramAddress, TAccountFounder, TAccountRake>);
}

export type SetFounderInput<
  TAccountFounder extends string = string,
  TAccountRake extends string = string,
> = {
  founder: TransactionSigner<TAccountFounder>;
  rake: Address<TAccountRake>;
  newFounder: SetFounderInstructionDataArgs["newFounder"];
};

export function getSetFounderInstruction<
  TAccountFounder extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: SetFounderInput<TAccountFounder, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): SetFounderInstruction<TProgramAddress, TAccountFounder, TAccountRake> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    founder: { value: input.founder ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("founder", accounts.founder),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getSetFounderInstructionDataEncoder().encode(
      args as SetFounderInstructionDataArgs,
    ),
    programAddress,
  } as SetFounderInstruction<TProgramAddress, TAccountFounder, TAccountRake>);
}

export type ParsedSetFounderInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    founder: TAccountMetas[0];
    rake: TAccountMetas[1];
  };
  data: SetFounderInstructionData;
};

export function parseSetFounderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetFounderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { founder: getNextAccount(), rake: getNextAccount() },
    data: getSetFounderInstructionDataDecoder().decode(instruction.data),
  };
}
