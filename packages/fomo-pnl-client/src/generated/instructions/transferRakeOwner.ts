/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findRakePda } from "../pdas";
import { FOMO_PNL_PROGRAM_ADDRESS } from "../programs";

export const TRANSFER_RAKE_OWNER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([40, 19, 221, 34, 163, 205, 7, 160]);

export function getTransferRakeOwnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRANSFER_RAKE_OWNER_DISCRIMINATOR,
  );
}

export type TransferRakeOwnerInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountRake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountRake extends string
        ? WritableAccount<TAccountRake>
        : TAccountRake,
      ...TRemainingAccounts,
    ]
  >;

export type TransferRakeOwnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  newOwner: Address;
};

export type TransferRakeOwnerInstructionDataArgs = { newOwner: Address };

export function getTransferRakeOwnerInstructionDataEncoder(): FixedSizeEncoder<TransferRakeOwnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newOwner", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TRANSFER_RAKE_OWNER_DISCRIMINATOR }),
  );
}

export function getTransferRakeOwnerInstructionDataDecoder(): FixedSizeDecoder<TransferRakeOwnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newOwner", getAddressDecoder()],
  ]);
}

export function getTransferRakeOwnerInstructionDataCodec(): FixedSizeCodec<
  TransferRakeOwnerInstructionDataArgs,
  TransferRakeOwnerInstructionData
> {
  return combineCodec(
    getTransferRakeOwnerInstructionDataEncoder(),
    getTransferRakeOwnerInstructionDataDecoder(),
  );
}

export type TransferRakeOwnerAsyncInput<
  TAccountOwner extends string = string,
  TAccountRake extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  rake?: Address<TAccountRake>;
  newOwner: TransferRakeOwnerInstructionDataArgs["newOwner"];
};

export async function getTransferRakeOwnerInstructionAsync<
  TAccountOwner extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: TransferRakeOwnerAsyncInput<TAccountOwner, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TransferRakeOwnerInstruction<TProgramAddress, TAccountOwner, TAccountRake>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.rake.value) {
    accounts.rake.value = await findRakePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getTransferRakeOwnerInstructionDataEncoder().encode(
      args as TransferRakeOwnerInstructionDataArgs,
    ),
    programAddress,
  } as TransferRakeOwnerInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountRake
  >);
}

export type TransferRakeOwnerInput<
  TAccountOwner extends string = string,
  TAccountRake extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  rake: Address<TAccountRake>;
  newOwner: TransferRakeOwnerInstructionDataArgs["newOwner"];
};

export function getTransferRakeOwnerInstruction<
  TAccountOwner extends string,
  TAccountRake extends string,
  TProgramAddress extends Address = typeof FOMO_PNL_PROGRAM_ADDRESS,
>(
  input: TransferRakeOwnerInput<TAccountOwner, TAccountRake>,
  config?: { programAddress?: TProgramAddress },
): TransferRakeOwnerInstruction<TProgramAddress, TAccountOwner, TAccountRake> {
  // Program address.
  const programAddress = config?.programAddress ?? FOMO_PNL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    rake: { value: input.rake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("rake", accounts.rake),
    ],
    data: getTransferRakeOwnerInstructionDataEncoder().encode(
      args as TransferRakeOwnerInstructionDataArgs,
    ),
    programAddress,
  } as TransferRakeOwnerInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountRake
  >);
}

export type ParsedTransferRakeOwnerInstruction<
  TProgram extends string = typeof FOMO_PNL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    rake: TAccountMetas[1];
  };
  data: TransferRakeOwnerInstructionData;
};

export function parseTransferRakeOwnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTransferRakeOwnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), rake: getNextAccount() },
    data: getTransferRakeOwnerInstructionDataDecoder().decode(instruction.data),
  };
}
